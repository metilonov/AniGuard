# No-groups account-first navigation v15

When a Telegram user has no connected groups, Mini App opens the user account page immediately.

The bottom navigation remains visible with exactly two global sections:

- Store
- Account

Group-related sections become visible automatically after at least one group is connected. The old no-groups intermediate screen and its duplicate action buttons are not used.
