# AniGuard — Working + Naruto MMO V2 merge report

## Основа

Сборка восстановлена на основе последней рабочей pre-Naruto линии AniGuard (целевое состояние до Naruto/MMO, commit `035f509ff32fef15ee8afc2e1e8738542ce7893c`). Для восстановления использована полная сборка AniGuard v23.2 и последовательность сохранённых pre-Naruto исправлений.

## Что сохранено

- основная модерация AniGuard;
- Telegram-группы и панель управления;
- веб-панель администратора (`app/static/admin.html`);
- основной Mini App (`app/static/index.html`);
- магазин (`app/store.py`);
- Premium аккаунта и группы (`app/premium_account.py` и связанные функции);
- media-safety для фото, видео, GIF и стикеров;
- права владельца группы и исправления регистрации;
- команды, русские алиасы, pin/unpin, ссылки на чат;
- все исходные функциональные файлы полной v23.2: 120/120 присутствуют, удалено 0.

## Что добавлено

Naruto RPG + Social MMO + MMO V2 интегрированы отдельным модулем `app/naruto_game/` без замены всей папки `app`.

Основные модули:

- `router.py`, `service.py`, `engine.py`, `models.py`, `content.py`;
- `extended.py`, `extended_router.py`;
- `social.py`, `social_router.py`;
- `advanced.py`, `advanced_router.py`;
- `integration.py`.

Клановые роли:

- 👑 Клан-лидер
- ⚔️ Джонин-командир
- 🕵 АНБУ-разведчик
- 💰 Хранитель свитков и казны
- 🥷 Элитный шиноби
- 🎓 Генин

## Проверки

- `compileall`: успешно;
- полный набор тестов проекта после актуализации устаревшего теста интерфейса: **115 passed**;
- Naruto/MMO-тесты отдельно: **23 passed**;
- базовые файлы v23.2: **120/120 присутствуют**, missing = 0.

## Ограничение проверки

Полный запуск Telegram/FastAPI-приложения в текущей рабочей среде не выполнялся, потому что в ней не установлен runtime-пакет `aiogram`. Все необходимые зависимости перечислены в `requirements.txt` и должны устанавливаться на хостинге/в виртуальном окружении проекта.

## Безопасное обновление

Эта сборка является **полным проектом**, а не патчем. В дальнейшем MMO-обновления нельзя применять способом, который заменяет всю папку `app` или фиксирует массовые удаления через `git add -A` без предварительной проверки `git status`.
